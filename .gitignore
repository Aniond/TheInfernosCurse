# ── GameMaker Studio 2 / GameMaker 2024 ─────────────────────────────────────

# Compiled output
*.win
*.ios
*.droid
*.linux
*.mac
*.ps4
*.xboxone
*.switch
*.uwp
*.tvos
*.gml.cache

# Cache and temp files
.cache/
*.tmp

# IDE build output
/builds/
/output/

# Local IDE settings that shouldn't be shared
*.user
local.settings.json

# Texture atlases — regenerated on build, can be large
/TextureGroups/

# Audio groups — regenerated on build
/AudioGroups/

# Runtime cache (GameMaker generates this)
*.yymps
*.yymp

# macOS artifacts
.DS_Store
__MACOSX/

# Windows artifacts
Thumbs.db
Desktop.ini

# IDE project lock (GameMaker creates this while open)
*.lock

# ── General ──────────────────────────────────────────────────────────────────
*.log
*.bak
*.orig

# ── Project docs (local reference only, not version controlled) ──────────────
docs/local/

# ── Misc root-level files ─────────────────────────────────────────────────────
*.winmd
*.exe
*.code-workspace

# ── External tooling (not game content) ──────────────────────────────────────
ComfyUI/

# ── Secrets / local credentials (never commit) ───────────────────────────────
config.ini
.mcp.json

# ── Local-only staging & reference material (not version controlled) ─────────
# Quarantine for strays / retired staging folders (installers, _lib_staging,
# _bg_backup, empty downloads tree — review and empty manually)
_archive/
# Library install staging scratch (zips + extracted GM libs)
_lib_staging/
# Reference PDFs (large rulebooks / grimoires, local reference only)
refrences/
references/
# Burst-test / debug screenshots (regenerated, large)
screenshots/
# Source image staging before GameMaker import
sprite_import/
# PixelLab API reference (local reference doc)
instructions/pixellab_api_reference.md
# Raw AI-generation concept dumps (kept on disk, not version controlled)
The Inferno's Curse/assets/concepts/characters/image.png
The Inferno's Curse/assets/concepts/characters/image (1).png
The Inferno's Curse/assets/concepts/characters/00008-2772151680.png
# Local map/worldbuilding reference images (download.jpg, florence.png — local only)
The Inferno's Curse/resources/
# Sprite-background-removal backups (safety copies before flood-fill, local only)
_bg_backup/

# Local dev/build/test workflow scripts (kept on disk, not version controlled)
clean_build.ps1
import_sprites.ps1
watch_burst.ps1
watch_game.ps1
instructions/watch_game.ps1
scripts/capture_hotkey.ahk
scripts/capture_screenshots.ps1
