// =============================================================================
// obj_door_placeholder — Create Event
// =============================================================================
// Trigger zone at the base of a building. No solid collision — player walks
// up to it and sees a prompt. Replace with real door object when art ships.

door_w       = 32;   // trigger zone width
door_h       = 16;   // trigger zone height
prompt_range = 100;  // world units — show prompt within this distance
near_player  = false;
