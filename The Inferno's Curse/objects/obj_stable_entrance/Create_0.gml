// =============================================================================
// obj_stable_entrance — Create
// =============================================================================
// Invisible interaction marker spawned by scr_room1_build() just south of the
// stable exterior sprite in Florence. Walk up to it and press E to enter
// Room_fiorentine_stable. (Spawned from code each Florence entry, so it never
// touches the saved layout — same pattern as obj_duomo_entrance.)
interact_range = 120;
player_near    = false;
