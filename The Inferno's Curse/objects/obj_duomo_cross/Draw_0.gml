// =============================================================================
// obj_duomo_statue — Draw
// =============================================================================
// Drawn via the shared rotation helper so the statue honours builder_angle and can
// be re-rotated with R in debug mode; unrotated draws normally.
scr_room_builder_draw_rotated(id, c_white);
