// =============================================================================
// obj_duomo_pew — Create
// =============================================================================
// A long bench in the nave. Solid (footprint built by scr_duomo_build_collision).
// All pews face NORTH toward the altar — they no longer overturn/tilt at high
// corruption (the congregation always reads as facing the altar).
