// =============================================================================
// obj_duomo_entrance — Create
// =============================================================================
// Invisible interaction marker spawned by scr_room1_build() just south of the
// Duomo exterior sprite in Florence. Walk up to it and press E to enter Room_duomo.
// (Spawned from code each Florence entry, so it never touches the saved layout.)
interact_range = 120;
player_near    = false;
